# 普通图片像素化评估（2026-09-19）

本次比较的是 **Plus 原网格恢复（`photo_mode=off`）与 Plus 新默认行为（`auto`）**，不是与 perfectPixel-main 比较。原 `grid.py`、`features.py`、`sampling.py` 未修改，新增逻辑集中在 `natural.py`。

## 实际输出

| 输入 | 原尺寸 | 只做原网格恢复 | 新默认输出 |
|---|---:|---:|---:|
| 有渐变、房屋、窗户、树木的插画 | 768×576 | 18×14 | 192×144 |
| 方形插画 | 1024×1024 | 1024×1024 | 256×256 |
| 透明背景插画 | 768×576 | 18×14 | 192×144 |
| 人物照片 | 512×600 | 512×600 | 128×150 |

前三张由测试文件的 `continuous_scene` 按固定种子生成；照片使用本机已有的 Matplotlib `sample_data/grace_hopper.jpg`，仅作人工观察，测试和运行不依赖 Matplotlib，也未下载素材。

结果保存在 [output/ordinary-evaluation](output/ordinary-evaluation/)。每张 `*-comparison.png` 从左到右是：

1. 输入。
2. Plus 原网格恢复（关闭新分支）。
3. Plus 新默认行为，不限颜色。
4. 新结果另加可选的 32 色后处理。

对比图按同样窗口适配显示，像素放大使用最近邻；标题标出原生尺寸。`*-auto.png` 是真正低分辨率输出，`*-auto-32colors.png` 是独立调色后的输出。处理过程位于 `output/debug/ordinary-evaluation/`。可直接查看 [插画对比](output/ordinary-evaluation/landscape-comparison.png) 和 [照片对比](output/ordinary-evaluation/photograph-comparison.png)。

## 已验证的保护行为

Python **261 项通过，34.17 秒**，包含新增的 34 项回归。测试仍集中于 `src/tests/test_pixelperfect.py`。

- input 六张原图与既有逐像素 SHA256 基准完全一致：bocchi 131×198、bocchi2 125×93、chito 111×166、hollow-knight 70×73、lastTour 331×219、ritsu 156×232。
- 六张图分别再施加 0.6 像素高斯模糊、JPEG quality=85、1.13 倍双线性缩放，共 18 个额外案例；新旧分支输出尺寸和每个像素相同。这里保证的是新功能不改变原恢复结果，不代表退化后的恢复结果一定是真值。
- 已知像素真值的最近邻放大、轻微模糊、JPEG、双线性放大、非整数缩放和平移裁剪，也都保留原恢复路径。
- 普通图片生成尺寸全幅覆盖、切线单调、固定输入重复运行逐像素一致；方形模式和非整除尺寸通过。
- 新分支不得比已有网格减少任何方向的格子数。两个方向重复格距的保护独立于是否存在较强全局起点对齐。
- 低对比织纹区域的面积平均误差低于中心采样，同时保留有源图支持的连续深色细线。
- 完全透明 RGB 不影响结果；小主体不会因为透明画布很大而进入粗网格；颜色后处理不改变初次恢复结果。
- CLI 开关、debug 标记、原生像素保护、PNG 导出、1–16 倍最近邻导出和已有颜色处理继续通过。

第一轮扩展测试发现：仅用全局对齐强度作为保护，会误分流部分再次模糊或缩放的伪像素画。现已加入双向重复格距的保留条件，以上 18 个案例全部加入断言，没有删除失败案例。

真实 Chrome 中 **31 组配置**的网页与 Python 输出 RGBA、切线及分流结果一致，包含普通插画、关闭新分支、32 色和透明插画。网页新增开关及重新生成、原有预览缩放/拖动、调色解耦、导出、主题/语言均通过。记录位于本地 `web/tests/results/verification.json`，截图为 `web/tests/results/ordinary-image.png`。网页核心通过 `python web/build.py --check`，与源文件逐字节一致。

## 实测性能

环境：Windows 11 x64，Python 3.12.11，NumPy 1.26.4，Pillow 12.1.0。浏览器为本机 Chrome，核心运行于 Pyodide；不同硬件与浏览器耗时会不同。

在其余验证结束后，对 **1024×1024 → 256×256** 插画进行一次独立 API 调用测量，不含测试图片构造、PNG/debug 写出和浏览器初始化：

| 阶段 | 秒 |
|---|---:|
| 读取、预处理 | 0.0148 |
| FFT 与边缘 | 0.0902 |
| 原网格检测 | 0.0213 |
| 新分流判断 | 0.0053 |
| 取色 | 0.2263 |
| 颜色后处理入口（未限色） | 0.0044 |
| 完整 pixelize 调用 | **0.3824** |

完整时间还包含结果图像和诊断数据构造，因此略大于表内分项之和。原始记录含各示例耗时及 24 个真实图片回归对照：[evaluation.json](output/ordinary-evaluation/evaluation.json)。这不是实时性能承诺，也没有把开启诊断和调色的成本算作零。

## 尚不稳定或未保证的情形

- 自动分流是保守启发式判断。颗粒噪声明显的照片、强重复纹理或建筑立面可能被保留为网格恢复；严重模糊到失去间距证据的伪像素画也可能误分类。
- 新分支不能保证原检测器本身一定正确；没有真实网格的 AI 图片不能用输出大小或重建误差充当正确率。
- 96–256 长边预算偏向保留细节；结果可能仍比手工绘制的 32×32/64×64 像素画细。透明主体保护还可能提高尺寸。
- 采样不理解物体语义，不会自动重画面部和线条。远离采样中心、低于最终像素尺寸的细节可能丢失，曲线阶梯和渐变色带仍可能出现。
- 可选低色数会增强色块感，也可能损失五官、高光或出现色带；默认保持不限颜色，不把调色混入网格判断。
- 普通图片的网格是生成的取样布局，标记 `stylized=true`，置信分数为 0，不声称找回唯一的原始像素网格。

不适合新分支的图片可在网页“更多设置”取消“普通图片自动像素化”，或使用 `--photo-mode off`。

## 2026-09-19：相机 JPG 与大图内存修复（历史记录）

本次保持网格选择、中心支持采样与普通图片分流规则不变，调整图片读取、FFT/梯度中间缓冲、原尺寸回退和诊断绘图的内存用法。网页 `core.zip` 已同步并通过逐字节检查。

### 验证通过的行为

- **279 项 pytest 通过**，包括原有 6 张输入的固定像素基准、新增奇偶尺寸和跨条带边界的梯度/FFT/频谱投影一致性、Pillow 颜色模式转换、MPO 主图选择及调用者帧位置保留、原尺寸回退不修改原图。动画 GIF/APNG、多页 TIFF 仍明确拒绝。
- **31 组真实 Chrome 对照通过**：PNG 输出全部 RGBA、格线与桌面一致，启发式分数差小于 1e-6。引擎预热、取消/重试、调色解耦、导出、诊断和界面操作继续通过。
- 在浏览器内注入真实 Python `MemoryError`，验证中文错误提示、旧 Worker 释放，以及下一次生成重新初始化并成功；这项故障注入不代表真实内存容量测试。
- 另外在同一个真实浏览器 Worker 中，依次完成小 MPO/JPG、8256×5504 JPG、同尺寸 MPO/JPG、同尺寸 PNG 回退、再次处理大 JPG；这些是实际分配内存的运行，全部开启处理过程输出，没有缩小输入。

### 大图实测

环境：Windows x64、本机 Chrome，桌面 Python 3.12.11 / NumPy 1.26.4 / Pillow 12.1.0；浏览器 Pyodide Python 3.12.1 / NumPy 1.26.4 / Pillow 10.2.0。使用程序绘制的房屋、窗户、树木、太阳、地面图案，最近邻或平滑放大到 8256×5504，分别保存 PNG、JPEG、含附图的 MPO/JPG。这不是用户的尼康原片，也不作为真实照片的风格质量评估。

| 8256×5504 输入 | 网页输出 | 桌面处理/PNG/诊断（秒） | 浏览器处理/PNG/诊断（秒） | 观察到的 Wasm 堆容量（MiB） |
|---|---|---:|---:|---:|
| 平滑放大 JPEG | 256×171，普通图片分支 | 9.83 | 8.91 | 1794.25 |
| MPO/JPG 主照片 + 附图 | 256×171，普通图片分支 | 9.81 | 8.29 | 1794.25 |
| 最近邻 PNG，证据不足回退 | 8256×5504 | 8.03 | 7.07 | 1892.81 |

计时是单次 `total_with_export`，含结果 PNG 和诊断绘图，不含初始化、原图预览 PNG 编码、消息传输和界面绘制。堆容量来自阶段间读取 Wasm 已分配内存，不等于浏览器进程总内存或精确的活动数组峰值；同一 Worker 的堆增长后通常不立即缩回。全部记录与桌面生成的结果/处理过程位于 [memory-regression](output/debug/memory-regression/large-images.json)。当前引擎堆上限为 2048 MiB，测试已接近限制，不能据此保证任意 45MP 输入、限色设置或设备都能运行。

在 2832×1316 数值对照中，用 `tracemalloc` 测得特征阶段额外数组峰值由 **100.57 MiB 降至 82.82 MiB**（不含输入缓冲）；本轮时间为 0.310 秒与 0.387 秒。条带计算有循环开销，本次主要收益是降低内存峰值。修改前后梯度、完整对数频谱、方向频谱投影、边缘投影和曲率投影全部逐元素一致。原始测量见 [fft-memory.json](output/debug/memory-regression/fft-memory.json)。

### 保留的失败与差异

- 最近邻房屋图案缺少足够的细格边缘，当前检测器没有恢复其 32 倍缩放格，按原策略保留原尺寸。本次验证其内存回退与像素完整性，没有修改网格算法或把回退称为准确恢复。
- 合成 JPEG/MPO 在两个 Pillow 构建中解码像素不同。小 MPO 样例的原始解码 RGB 最大通道差为 40；桌面回退 258×172，浏览器选择 43×29。两个大 JPEG/MPO 的输出尺寸与格线相同，但颜色不完全相同。将浏览器解码后的源图保存为无损 PNG，再由桌面处理，这三个样例的结果全部逐像素一致；因此记录为编解码输入差异，不声称所有 JPEG 跨平台逐字节一致。
- 没有拿到用户的尼康原片，不能仅凭扩展名和相机品牌确认它含有 MPF/MPO。新增格式/帧数诊断可用于核实；已用真实编码的 MPO 容器验证主图读取规则。
- 大图保留原尺寸、颜色后处理、高倍导出仍可能超过浏览器内存；错误后可恢复不等于无限制支持大图。桌面 Python 没有该 Wasm 堆上限，但同样受设备可用内存约束。

## 2026-09-20：默认像素化回退与大图加速（当前行为）

此前“全局网格未通过验证”与“边缘很锐利”同时出现时，分流会拒绝普通像素化，导致像素风图片仍按原尺寸输出。现在没有可靠候选时直接启用生成网格，不再把边缘锐利作为拒绝理由。原生单像素细节、小尺寸和小透明主体仍受保护；`photo-mode=off` 保留不做回退像素化的选择。

同时补充了原始边缘的整数重复检查：在常规候选失败时，检查两个方向全部已采样变化坐标的整数公约数及起点。它恢复了此前被拒绝的 40 倍建筑图样，并让上一节 8256×5504 的 32 倍房屋图恢复为 **258×172**，不再输出原始大图。

### 验证与修复中发现的问题

- **284 项 pytest 通过，34 组 Chrome/Python PNG 对照通过**。原有 6 张实际输入默认输出的固定 RGBA 哈希保持一致；原生结果重传、18 个退化副本、颜色后处理、透明度、导出和网页交互继续通过。
- 新增明确没有可信全局格子的锐利屋顶图，1024×768 自动输出 **256×192**，置信分数为 0，并保留透明背景。关闭自动像素化时仍保留原图。
- 超过 400 万像素的 35/40 倍建筑图都恢复到已知 **67×57** 真值，逐像素一致。扫描线的一维坐标长度仍等于完整源图宽高，未使用缩小图间距。
- 仅放开旧的锐利边缘限制，会误压缩一幅含真实一像素窗格的大画布；已修复为依据单像素证据保护。直接采用所有锐利反转又会误把噪声标为原生像素，因此增加相邻近同色区域的要求，原有噪声拒绝测试保持通过。
- 64 个区域样本可能恰好绕过格子中心的细线，已增加受支持中心色对平均色的否决条件；新增 2048×2048 连续细线回归验证，没有删除失败用例。

### 实测性能

同一台 Windows x64 电脑，Chrome / Pyodide Python 3.12.1 / NumPy 1.26.4 / Pillow 10.2.0。使用上一节相同的 8256×5504 合成文件；引擎已初始化、原图预览已显示，保持默认不限色，**开启生成处理过程**。网页预览复用经过实际检查，算法输入仍是原文件字节。

| 输入 | 上版处理 + PNG/诊断 | 本版处理 + PNG/诊断 | 本版点击生成到结果可用 | 输出 |
|---|---:|---:|---:|---|
| 平滑放大 JPEG | 8.91 秒 | **3.52 秒** | **3.63 秒** | 256×171，自动像素化 |
| 干净整数放大 PNG | 7.07 秒 | **2.88 秒** | **3.03 秒** | 258×172，检测整数重复 |

JPEG 处理计时减少约 **60.5%**。数据为单次实测，可能受机器负载影响；不包含首次引擎启动和选图解码，不承诺所有图片都有同样速度。阶段记录和真实网页截图见 [browser.json](output/debug/fast-fallback/browser.json)。旧版重新编码原图预览的成本不在“处理 + PNG/诊断”计时中，本版还通过复用已显示原图减少了该额外成本。

加速来自：超过 400 万像素时改用每轴 96 条完整长度扫描线、展示用中心 512×512 FFT、每格 64 个区域样本的平滑统计，以及避免重复编码浏览器已经成功显示的普通原图。可靠网格仍走原有采样模块，不采用这组近似区域统计。

### 当前限制

- 这仍是确定性的检测与重采样，自动回退网格不是恢复出的原网格；无可靠证据时明确标记生成网格、分数 0。
- 大图扫描线可能漏掉仅存在于未采样窄条中的局部结构；规则纹理可能干扰区域采样，400 万像素阈值两侧可能给出不同候选。不保证与旧版全图 FFT 的所有输出相同。
- 本节测试时尚未获得聊天附件原文件，只验证了同类分支。随后取得原文件后的检测修复记录见下一节。
- JPEG 解码器差异和浏览器内存上限仍存在；附加色库、很大的原生像素图或高倍导出可能增加成本。

## 2026-09-20：实际人物图的候选格距遗漏

本次使用用户放入的 [1770467992323.jpg](input/1770467992323.jpg)，1024×1024。扩展名虽为 JPG，Pillow 确认实际编码为 PNG。以下均是该原图的运行结果。

### 原因与修复

原 Plus 的横纵边缘投影对比度分别为 0.837 / 0.767，共找到 85 / 109 个投影峰；因此并非没有边缘，也不是触发了原生 1×1 保护。候选格距 10.6667 经局部格线修正后的分数为 **0.301954**，低于接受门槛 **0.31**，于是进入普通图片像素化，输出 256×256、置信分数 0。

原 main 的 NumPy 后端认为 FFT 格距不一致，改用梯度间距：横纵估计为 13 / 10 像素，平均 **11.5 像素**，经过自身格线修正后输出 **86×86**。它不使用 Plus 的上述分数门槛。

修复补上有限的邻近格距搜索，而不是降低门槛。该图额外比较 11 个候选，最终 **10.5×10.5** 的名义格距、**99×99** 网格得到 **0.320228**；局部修正后实际格宽/高的中位数为 10 像素，覆盖完整输入。格距附近的变化会改变局部边缘吸附结果，所以只调整起点并不能替代这一步。采样、alpha 和颜色后处理均未修改，已接受网格不会进入新增搜索。

- [Plus 原生结果](output/1770467992323.png)
- [main / Plus 对比图](output/debug/grid-investigation/comparison/comparison.png)：左 main 86×86，右 Plus 99×99，均为 4 倍最近邻展示、白底；各自程序使用默认参数和自身检测。
- [修复前数据](output/debug/grid-investigation/plus-before.json) / [修复后过程与格线](output/debug/1770467992323/info.json)

### 验证与性能

**288 项 pytest 全部通过，43.95 秒。** 新增原图、镜像、轻微裁剪和轻微模糊四个实际回归，均退出普通像素化，输出范围 98～104 像素；验证覆盖、格线单调、原有门槛、低置信提示、重复运行与关闭普通像素化后的输出一致。原 6 张图片的固定像素摘要、原生小图保护、噪声拒绝、照片回退、颜色解耦等断言未放宽。

**35 组真实 Chrome 对照通过**，包含该原文件：浏览器输出也是 99×99，全部输出 RGBA、格线与桌面一致，启发式分数误差小于 1e-6。启动、错误重试、界面、调色、导出及本地请求检查通过；`web/core.zip` 与源代码逐字节同步。新增第二个开启诊断的输入时，发现浏览器测试把压缩包路径写死为 lastTour，已改为逐个保存并严格验证每个输入自己的 7 个过程文件，未放宽断言。记录见 [browser-verification.json](output/debug/grid-investigation/browser-verification.json)。

Windows x64 / Python 3.12.11 / NumPy 1.26.4 / Pillow 12.1.0 单次实际 API 计时：修复前 **0.392 秒**，修复后 **0.204 秒**。后者检测 0.065 秒、取色 0.041 秒，包含结果 PNG 和处理过程绘制为 **0.564 秒**。这是同图单次测量，不是稳定速度倍数的承诺；较小恢复网格减少了回退取色工作。

浏览器 Pyodide Python 3.12.1 / NumPy 1.26.4 / Pillow 10.2.0，此图单次处理含结果 PNG 和诊断绘制为 **2.627 秒**，不含引擎初始化和 UI 消息传输。这是浏览器对照测试计时，不能用桌面的 0.204 秒代替网页耗时。

该图不存在已知低分辨率真值，局部格距也不完全一致，不能凭输出尺寸或以上分数声称精确还原、更高正确率。仍显示低启发式置信度（约 0.32），只是明确采用边缘支持的近似网格，不再把固定预算的普通像素化当作该图的恢复结果。邻域搜索仍可能遗漏较远尺度或受重复纹理干扰。

### 后续调整：接受门槛改为 0.30

按用户要求，初始候选与邻近格距细化统一使用 **0.30** 接受门槛；细化触发范围对应改为 `[0.25, 0.30)`，双向间距支持门槛 0.10、低置信提示门槛 0.45 不变。该图原候选 0.301954 现在直接通过，名义格距 10.6667，输出 **96×96**，不再触发额外格距细化。上述 99×99 属于前一版门槛 0.31 的历史结果；结果 PNG、过程文件及 main/Plus 对比图已更新为本版 96×96。

本次 **288 项 pytest 通过（36.76 秒）**，包含原图直接接受、裁剪/模糊变体继续细化、原有六张图片像素基准及噪声/普通图片回退。网页核心重新打包并通过逐字节同步检查；上一节 35 组浏览器记录对应门槛 0.31，本次未重新测量浏览器性能。
